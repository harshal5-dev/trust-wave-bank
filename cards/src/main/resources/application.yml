server:
  port: 9002

spring:
  application:
    name: cards
  profiles:
    active: "prod"
  datasource:
    url: jdbc:postgresql://localhost:5436/cardsdb
    username: root
    password: 12345
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
#  sql:
#    init:
#      mode: always
  config:
    import: "optional:configserver:http://localhost:8071/"
#  rabbitmq:
#    host: "localhost"
#    port: 5672
#    username: "guest"
#    password: "guest"

management:
  endpoints:
    web:
      exposure:
        include: "*"