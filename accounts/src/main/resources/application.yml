
server:
  port: 9001

spring:
  application:
    name: accounts
  profiles:
    active: "prod"
  datasource:
    url: jdbc:postgresql://localhost:5434/accountsdb
    username: root
    password: 12345


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  #  sql:
#    init:
#      mode: always
  config:
    import: "optional:configserver:http://localhost:8071/"
#  rabbitmq:
#    host: "localhost"
#    port: 5672
#    username: "guest"
#    password: "guest"

management:
  endpoints:
    web:
      exposure:
        include: "*"